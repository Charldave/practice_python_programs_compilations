#ask user input
#ask user character to find

#found_index = -1

#for count in len(user input)
    #if user input == character to find
        #found_index = count
        #break

#if found_index != -1
    #print found_index
#else
    #print "not found"